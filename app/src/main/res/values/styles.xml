<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="TextInputLayout"
        parent="Wide" />

    <style name="Toolbar"
        parent="Wide">
        <item name="android:layout_height">?attr/actionBarSize</item>
        <item name="popupTheme">@style/ThemeOverlay.MaterialComponents.AppTheme</item>
        <item name="android:background">@color/colorPrimary</item>
    </style>

    <!-- Authentication -->

    <style name="TextInputLayout.Authentication">
        <item name="android:paddingStart">@dimen/default_margin</item>
        <item name="android:paddingEnd">@dimen/default_margin</item>
    </style>

</resources>